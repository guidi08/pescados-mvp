# Clawdbot tasks (automação sugerida)

Veja `docs/CLAWDBOT_AUTOMATIONS.md`.

Resumo:
- gerar arquivos .env a partir de exemplos
- deploy API + portal via CLI (Render/Vercel)
- criar webhook Stripe via CLI em staging
- rodar smoke tests

Tarefas que precisam de humano: `docs/HUMAN_TASKS.md`.
